# Python 编程基础讲义项目规则

## 项目定位

- 当前仓库用于本学期 Python 编程基础课程的教学讲义。
- 课程内容按以下目录组织，新增讲义应放入对应目录，不随意改变现有目录结构：
  - `01-基础语法/`
  - `02-数据结构/`
  - `03-函数与模块/`
  - `04-文件与异常/`
  - `05-面向对象/`
  - `06-基础项目/`
  - `拓展-办公自动化/`
  - `拓展-Web与爬虫/`
  - `拓展-数据分析/`
  - `拓展-机器学习/`
  - `archive/`

## 讲义内容规则

- 修改讲义时只处理用户明确指定的章节，正文内容和图片引用不得无关改动。
- 每篇讲义结尾统一使用标题 `### 知识点总结`，不再使用简单的 `### 总结`。
- “知识点总结”必须覆盖本篇全文的主要知识点，包括概念、操作方法、代码要点、命名规则、注意事项和学习重点；不能只写三五句泛泛概括。
- 代码示例应保持 Python 初学者可读，优先使用清晰、短小、可直接运行的示例。
- 新增或调整图片时，优先使用相对路径，并将图片放入对应章节的 `assets/` 目录。
- 讲义中的 LaTeX 公式（如 `$...$`、`\(...\)`、`$$...$$`、`\[...\]`）应在网页中通过 MathJax 正常渲染，不能以原始 LaTeX 字符串直接展示。
- 不顺手修复与当前任务无关的历史内容，发现问题时记录并向用户说明即可。

## 网页发布规则

- 教学网页统一发布到 Cloudflare Pages 项目 `python-notes`，后续课程讲义继续上传到这个项目，不为每个课时或阶段新建项目。
- 稳定访问地址为：`https://python-notes.pages.dev`。
- 不要覆盖或替代已有的 `teaching-docs` 项目。
- 网站采用静态 HTML 发布：将 Markdown 讲义转换为网页，并复制所需的章节 `assets/` 图片；原始 Markdown 文件仍作为仓库正文保留。
- 发布前必须验证首页和各讲义页面能正常访问，并检查图片、代码块、页内跳转和移动端布局。

## 网页视觉与交互规则

- 网站总标题使用 `Python 编程基础`，不得在总标题中写入“第 1–3 讲”等阶段范围。
- 网站副标题使用 `Python 基础课程讲义`，不得写入阶段范围。
- 页脚署名固定为 `by天才程序员`。
- 讲义页面正文左侧增加“本页目录”侧栏，自动提取二级和三级标题生成可点击锚点。
- 页内目录应支持：点击跳转、回到顶部、滚动时保持可见；窄屏设备自动切换为正文上方的目录。
- 单篇导航可以保留“第 1 讲”“第 2 讲”等单篇编号，但网站总标题、副标题、首页说明和页脚不得出现阶段范围字样。
- 标题文字应保持高对比度和清晰可读，页眉使用白色标题、渐变背景、适度字距和阴影。

## 发布流程

1. 先修改并检查对应 Markdown 讲义。
2. 生成静态网页和页内目录，保留原始讲义文件不变（除用户要求的内容修改外）。
3. 使用 Wrangler 将构建目录部署到 `python-notes`。
4. 发布后验证 `https://python-notes.pages.dev` 及新增页面的 HTTP 状态、标题、目录锚点和资源加载情况。
5. 临时构建脚本和临时目录不应残留在仓库中，除非用户明确要求保留。
