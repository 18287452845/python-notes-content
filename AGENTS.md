# Python 编程基础讲义项目规则

## 仓库边界

- 本仓库是 **Markdown 内容仓库**（GitHub: `18287452845/python-notes-content`），只维护讲义正文、章节图片和课程目录。
- 网页模板、构建脚本、生成 HTML 和 Cloudflare 部署在独立仓库 [`python-notes-site`](https://github.com/18287452845/python-notes-site)。不要在本仓库执行 Wrangler 部署，不要提交生成 HTML，也不要覆盖或替代已有的 `teaching-docs` 项目。
- 内容提交到 `main` 后，由 site 仓库定时拉取并重建站点（约 15 分钟）；若配置了 `SITE_REPO_DISPATCH_TOKEN`，本仓库的 `.github/workflows/notify-site-sync.yml` 会在 md/assets 变更后立即通知同步。完整同步链路见 `README.md`。
- 部署目标始终是现有 Cloudflare Pages 项目 `python-notes`（稳定地址 `https://python-notes.pages.dev`），不要为新课时或新阶段新建项目。

## 目录与文件

- 课程内容按现有目录组织，新增讲义放入对应章节，不随意改目录结构：
  - `01-基础语法/` → `02-数据结构/` → `03-函数与模块/` → `04-文件与异常/` → `05-面向对象/` → `06-基础项目/`
  - `拓展-办公自动化/`、`拓展-Web与爬虫/`、`拓展-数据分析/`、`拓展-机器学习/`
  - `archive/`：归档与快照，不是当前在用讲义
- 单篇命名习惯为 `NN.标题.md`；合课可用 `32-33.标题.md` 这类区间编号。
- 图片放对应章节的 `assets/`，讲义中用相对路径引用。
- 基础章（01–06）不依赖第三方库；`requirements-basics.txt` 故意为空，拓展章依赖按讲义单独说明，不要为“补依赖”而改该文件。
- `.wrangler/` 是本地缓存且已忽略；根目录临时脚本（如分析用的 `.extract_*.py`）不属于课程内容，不要当正式交付物保留或扩散。

## 讲义内容规则

- 只处理用户明确指定的章节；正文和图片引用不要顺手改动无关内容。发现问题时记录并向用户说明即可。
- 每篇结尾统一用 `### 知识点总结`（不用简单的 `### 总结`）。总结须覆盖概念、操作方法、代码要点、命名规则、注意事项和学习重点，不能只写三五句泛泛概括。
- 代码示例保持 Python 初学者可读：短小、清晰、可直接运行，并带语言标记（如 ` ```python `）。不要保留无教学作用的 `Version: 1.0`、`Author: XX` 模板信息。
- 数学公式用 MathJax 支持的 `$...$`、`$$...$$`、`\(...\)`、`\[...\]`，不要让网页把原始 LaTeX 当普通文本展示。

## 网页相关约定（改站点时仍须遵守）

- 站点总标题：`Python 编程基础`；副标题：`Python 基础课程讲义`；页脚：`by天才程序员`。总标题、副标题、首页说明和页脚不得出现“第 1–3 讲”等阶段范围字样；单篇导航里的“第 N 讲”可以保留。
- 讲义页应有“本页目录”侧栏（自动提取二级、三级标题），支持点击跳转、回到顶部、滚动固定；窄屏时目录改到正文上方。
- 页眉保持白色标题、渐变背景、适度字距与阴影，保证对比度可读。
- 若需要改布局、目录交互、代码高亮或构建逻辑，应提交 **site 仓库**；本仓库只改 Markdown 与 assets。
